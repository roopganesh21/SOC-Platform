# Backend environment variables (example values)

# Port the Express server listens on
PORT=5100

# Node environment
NODE_ENV=development

# SQLite database path (relative to backend/)
DB_PATH=./db/soc.sqlite

# CORS allowed origin (use * for all in development)
CORS_ORIGIN=http://localhost:3000

# Rate limiting (15 minutes window, max 100 requests per IP by default)
RATE_LIMIT_WINDOW_MINUTES=15
RATE_LIMIT_MAX=100

# Google Generative AI / Gemini API key (for AI explanations)
GEMINI_API_KEY=AIzaSyCJm4cnalSq8h-NIojdJsk-OburXE7snsM
